# Sirizone Marketplace - LM Studio Integration

## نظام الدعم الذكي AI Support System

تم إضافة نظام دردشة ذكي متصل بـ **LM Studio** للرد على استفسارات العملاء.

---

## 📋 خطوات التشغيل

### 1. تشغيل LM Studio

1. افتح **LM Studio**
2. حمّل نموذج AI (مثل: Mistral, Llama, أو أي نموذج آخر)
3. اذهب إلى تبويب **Server**
4. اضغط على **Start Server**
5. تأكد من أن السيرفر يعمل على المنفذ **1234** (الافتراضي)

### 2. تشغيل الموقع

```bash
npm run dev
```

الموقع سيعمل على: `http://localhost:3001`

---

## 💬 استخدام نظام الدردشة

- ستجد زر **💬** في الزاوية اليمنى السفلية من الموقع
- اضغط عليه لفتح نافذة الدردشة
- اكتب سؤالك واضغط Enter أو زر الإرسال
- سيرد عليك AI بناءً على نموذج LM Studio

---

## ⚙️ الإعدادات

يمكنك تعديل إعدادات LM Studio في ملف `.env.local`:

```env
LM_STUDIO_URL=http://localhost:1234/v1/chat/completions
```

إذا كنت تستخدم منفذ مختلف، غيّر الرابط.

---

## 🧠 تخصيص الردود

لتخصيص شخصية AI أو طريقة الردود، عدّل ملف:
```
src/app/api/chat/route.ts
```

في قسم `system` message:
```typescript
{
  role: 'system',
  content: `أنت مساعد دعم فني لـ Sirizone Marketplace...`
}
```

---

## 🔧 المشاكل الشائعة

### ❌ "Failed to connect to AI service"
- **الحل**: تأكد من تشغيل LM Studio Server على المنفذ 1234

### ❌ الردود بطيئة
- **الحل**: استخدم نموذج AI أصغر حجماً (مثل Mistral 7B بدلاً من 13B)

### ❌ الردود غير دقيقة
- **الحل**: جرب نموذج AI مختلف أو عدّل الـ system prompt

---

## 📁 الملفات المضافة

1. `/src/app/api/chat/route.ts` - API endpoint للاتصال بـ LM Studio
2. `/src/components/common/AIChatWidget.tsx` - واجهة الدردشة
3. `.env.local` - إعدادات LM Studio
4. التعديل على `layout.tsx` - لإضافة الويدجت

---

## 🚀 المميزات

✅ دردشة في الوقت الفعلي  
✅ يعمل بدون إنترنت (offline)  
✅ خصوصية كاملة (لا ترسل بيانات لخوادم خارجية)  
✅ ردود ذكية بناءً على نموذج AI  
✅ واجهة سهلة وجميلة  
✅ دعم متعدد اللغات (عربي/إنجليزي)  

---

## 📞 للدعم

إذا واجهت أي مشاكل، تأكد من:
1. LM Studio يعمل ويستجيب
2. السيرفر على المنفذ الصحيح
3. النموذج محمّل بشكل صحيح
